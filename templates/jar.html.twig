{% extends 'base.html.twig' %}

{% block body %}
        <h1>{{ number }} <i class="fas fa-coins"></i> | {{ number * 0.20 }} <i class="fas fa-euro-sign"></i></h1>

        <!--{{ form(addF) }}{{ form(subF) }}-->


        <button onclick="location.href='http://localhost:8000/add'"> <i class="fas fa-plus"></i> </button>

        <button onclick="location.href='http://localhost:8000/sub'"> <i class="fas fa-minus"></i> </button>
{% endblock %}